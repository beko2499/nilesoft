services:
  - type: web
    name: nilesoft
    runtime: static
    buildCommand: 'true'
    staticPublishPath: ./
    envVars:
      - key: SKIP_INSTALL_DEPS
        value: 'true'
    headers:
      - path: /assets/*
        name: Cache-Control
        value: public, max-age=31536000, immutable
